import { NdrFilter } from 'apis/get'

type Props = {
    filters: NdrFilter[] // this can be an empty array if network call fails
}

export default function PageFilters({}: Props) {
    return <>Hi</>
}
